export const formatMoyenne = (m: any) => {
  return Number(m).toFixed(2);
};
export const formatRang = (m: any) => {
  return Number(m).toFixed(0);
};

export const formatPourcentage = (m: any) => {
  return Number(m).toFixed(2);
};
